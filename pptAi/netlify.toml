[build]
  command = "npx prisma generate && vite build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"

[functions]
  included_files = ["src/generated/prisma/**"]

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
