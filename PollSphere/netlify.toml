[build]
  # Running from root with explicit prefix to ensure dependencies are installed
  command = "npm install --prefix PollSphere/frontend && npm run build --prefix PollSphere/frontend"
  
  # Pointing to the dist folder relative to the repo root
  publish = "PollSphere/frontend/dist"

[build.environment]
  NODE_VERSION = "20"
